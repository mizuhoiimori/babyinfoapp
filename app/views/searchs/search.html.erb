<div class='container-fluid'>
  <div class='row'>
    <% if @model == "user" %>
      <h2>ユーザー検索"<%= @content %>"</h2>
      <table class='table'>
        <thead>
          <tr>
            <th>アイコン</th>
            <th>ユーザー名</th>
            <th>自己紹介</th>
          </tr>
        </thead>
        <tbody>
          <% @records.each do |record| %>
            <tr>
              <td><%= attachment_image_tag(record, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %></td>
              <td><%= record.name %></td>
              <td><%= record.introduction %></td>
          <% end %>
        </tbody>
      </table>
    <% else @model == "item" %>
      <h2>アイテム検索"<%= @content %>"</h2>
  		<table class='table'>
    		<thead>
          <tr>
            <th colspan="1"></th>
            <th>商品名</th>
            <th>レビュー</th>
          </tr>
        </thead>
        <tbody>
          <% @records.each do |record| %>
    		  <tr>
    		    <td class="col-3">
              <%= attachment_image_tag(record.user, :profile_image, :fill, 50, 50, fallback: "no-image-icon.jpg") %>
            </td>
            <td>
                <%= link_to record.title, item_path(@records) %>
            </td>
            <td>
                <%= record.body %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    <% end %>
  </div>
</div>